### IF-ELSE
> control statement.
```
if boolean_expression1 {
    // code to execute if boolean_expression1 is true
} else if boolean_expression2 {
    // code to execute if boolean_expression1 is false and boolean_expression2 is true
} else {
    // code to execute if boolean_expression1 and boolean_expression2 are false
}
```
